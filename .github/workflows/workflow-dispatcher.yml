name: Workflow dispatcher
on:
  workflow_dispatch: 
    inputs: 
      env:
        required: true
        type: string
        
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy build to target
        if: ${{ contains(fromJSON('["dv01","dv02","dv03","dv04"]'), '${{ inputs.env }}') }}
        run: echo 'Hello ${{ inputs.env }}!'